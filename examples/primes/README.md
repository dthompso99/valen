# Prime counter

Counts prime numbers through 10,000 using trial division. This intentionally straightforward program
demonstrates functions, integer arithmetic, loops, and deterministic CPU-bound work.

```sh
./valen examples/primes/primes.ar -o primes
./primes
```

Expected output is `primes through 10000: 1229`.

