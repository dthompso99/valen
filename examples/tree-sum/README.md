# Tree sum

Allocates a complete binary tree and recursively sums its nodes. It demonstrates owned optional
references, recursive construction and traversal, and bounded managed allocation.

```sh
./valen examples/tree-sum/tree-sum.ar -o tree-sum
./tree-sum
```

Depth 10 produces 2,047 nodes, so expected output is `tree nodes: 2047`.

